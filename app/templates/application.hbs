{{page-title 'B-messages'}}
<div class='h-screen bg-blue-50 text-blue-700'>
  <div class='container mx-auto h-full'>
   <BasicNav @model={{this.user}}/>
    <div class='h-5/6'>
      {{outlet}}
    </div>
  </div>
</div>